.bulk-action
  %span.checkbox=check_box_tag "select_all"
  .actions
    %span=link_to "verwerfen", "#", :class => "small_action_button"
  %span.paginate
    = page_entries_info @leads
    = will_paginate @leads