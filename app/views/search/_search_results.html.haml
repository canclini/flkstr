#search_result
  #companies
    - for company in @companies
      = render company
      = paginate @companies
  #requests
    - for request in @requests
      = render request
      = paginate @requests