- for message in @messages
  =link_block(message_path(message)) do
    .actionbox_entry
      .title= message.author.name
      = truncate(message.subject, :length => 45)

-if @messages.empty?
  %span Keine Nachrichten