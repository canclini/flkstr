- title ("Neues Firmenprofil")

.leftmiddle
  =form_for @company do |f|
    = f.error_messages
    %p  
      = f.label :name, "Company Name"#, :id => "companycreate"
      %br
      = f.text_field :name, :class => "text focus"

    %hr
    = f.fields_for :user do |u|
      %p
        = u.label :email
        %br/
        = u.text_field :email
      %p
        = u.label :password
        %br/
        = u.password_field :password
      %p
        = u.label :password_confirmation
        %br/
        = u.password_field :password_confirmation


      = f.submit "Jetzt Registrieren", :class => "button"
  = render :partial => 'existing_companies'
.right
  .box
    #newcompanyexplanation
      %h3 Firma registrieren
      %p Im nebenstehenden Feld einfach deinen Firmennamen eingeben und mit "Jetzt Registrieren" die Firma erstellen und zum nächsten Schritt weitergehen.
      %p Unten erscheinen bereits registrierte Firmen mit ähnlichen Namen. Sollte deine Firma da bereits aufgelistet sein, kannst du mit einem Klick mehr darüber erfahren.