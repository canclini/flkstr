= form_tag new_message_path(), :id => "associates_form", :method => :put do
  = render :partial => 'bulk-action'
  = form_for companies_path do |f|
    = field_set_tag do
      = text_field :companies, :search, :class => "search"
      = f.submit "Suchen", :class => "search_button"
  
  = render :partial => 'list'
  = render :partial => 'bulk-action'